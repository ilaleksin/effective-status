services:
    db:
        image: postgres:13-alpine
        environment:
            POSTGRES_DB: status_page
            POSTGRES_USER: postgres
            POSTGRES_PASSWORD: passw
            POSTGRES_HOST_AUTH_METHOD: trust
        # ports:
        # - "5432:5432"
    # app:
    #     image: ialexin/effective-status
    #     ports:
    #     - "9091:9091"